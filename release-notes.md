狐径 FoxPath v1.0.5 — 在控制面板里直接升级

## 下载

| 文件 | 说明 |
| --- | --- |
| `FoxPath.exe` | 主程序，绿色单文件，双击即用。版本号写在 exe 属性里（鼠标悬停 / 右键-属性-详细信息） |
| `version.json` | 升级信息，已装用户点「检查更新」时读取。**这个附件不能少**，否则老版本拿不到升级提示 |

## 这一版新增：在线升级

升级做进了网页控制面板，不用再手动下载替换：

- 点「检查更新」即走完整流程：读 `version.json` → 比对版本 → 下载新 exe → 校验 SHA256 → 覆盖并重启。
- 界面有版本迁移动画（`1.0.4 ====> 1.0.5`）、渐变色进度条、实时百分比与已下载字节；
  结束给出三种状态 —— 有新版本 / 已是最新 / 失败原因。
- 覆盖交给独立的 PowerShell 小助手：先让旧程序退出并**还原系统代理**，确认进程消失后再替换文件、
  重新启动。不会出现「文件被占用、覆盖不了」。

接口：`POST /api/update/check`、`POST /api/update/start`、`GET /api/update/status`（沿用同源校验）。

## 从 v1.0.1 到这一版

`v1.0.2` ~ `v1.0.4` 是本机迭代版本，未单独发版，改动都并进这一版：

- **双击「打不开」** —— 程序已经在后台跑时，第二次双击以前只写一行日志就退出。程序是无窗口的、
  又没有托盘图标，屏幕上什么都不会出现，只能被判断成「坏了」。现在会把**正在运行的那个控制面板
  交给浏览器打开**；静默模式（开机自启）仍然安静退出。
- **图标重做** —— 原图是白底卡片、构图偏右下、边缘还留着一条线，缩到小尺寸糊成一团。
  改为去白底、清边框、裁到主体后居中放大，重出 16~256 七档 ICO。
- **产物命名** —— 文件名不再带版本号（固定 `FoxPath.exe`），版本号写进 exe 的版本资源。
- **作者署名** —— 源码文件头、控制面板底部、日志、exe 属性统一为「海风（kele551）」；
  README 里的私人邮箱换成仓库 Issues 链接。
- **体检日志带上运行时长** —— 长跑日志里能一眼看出是持续在跑，还是中途死过又重开。

## 用法

1. 下载 `FoxPath.exe`，双击运行
2. 浏览器自动打开控制面板 `http://127.0.0.1:8788/ui`
3. 勾选「开机自启」即可后台常驻

首次启动请等 15~20 秒（要并发体检 49 个候选 IP），体检完成前代理尚未完全生效。

## 已知边界

- 只代理 `github.com` / `www.github.com`；`githubassets.com` / `githubusercontent.com` /
  `api.github.com` 保持直连（走代理反而连不上，会让页面只剩裸 HTML、按钮点不动）
- 不能用 `nohup ... &` 在 shell 里后台启动，那样命令结束进程会被终止
- 控制面板只监听 `127.0.0.1`，但没有鉴权：本机上的其它程序或网页理论上可以调用它的接口
  （本地回环，风险很低；接口已做 Host/Origin 同源校验，挡网页 CSRF 与 DNS rebinding）
- 被强制结束时 `atexit` 不会执行，系统代理可能仍指向本程序；跑一次 `--restore` 收干净

## 版本历史

| 版本 | 日期 | 说明 |
| --- | --- | --- |
| v1.0.5 | 2026-09-22 | **网页版在线升级**：检查更新 / 立即升级 / 进度条，下载校验 SHA256 后由独立小助手接管覆盖重启（本次对外发布，Gitee 与 GitHub 同步） |
| v1.0.4 | 2026-09-22 | 作者署名统一为「海风（kele551）」；README 私人邮箱换 Issues 链接（本机迭代，未单独发版） |
| v1.0.3 | 2026-09-22 | 重做图标；产物固定 `FoxPath.exe`，版本号写进 exe 属性（本机迭代，未单独发版） |
| v1.0.2 | 2026-09-22 | 程序已在后台时，第二次双击改为直接打开正在运行的控制面板（本机迭代，未单独发版） |
| v1.0.1 | 2026-09-22 | 修「还原不了系统代理」一类缺陷：重复启用不再污染备份、备份失败不再改注册表、静默模式也注册退出还原、加单实例与端口占用提示、日志落文件、新增 `--restore`；`Fix-GitHub.ps1` 补 UTF-8 BOM（此前无法运行） |
| v1.0.0 | 2026-09-19 | 首个版本：本地代理 + PAC、每 5 分钟 IP 体检、候选池自动刷新、全灭自愈、退出还原 |

反馈：[仓库 Issues](https://gitee.com/kele551/FoxPath/issues)

---

维护约定（发版的人看）：这份文件的**首行版本号就是当前线上版本**。发新版前把它整篇换成新版本的说明
（或改用 `--notes <文件>`）；`tools/publish.py` 不带 `--notes` 时会读这份文件，并校验首行版本号与
要发的版本一致，不一致直接中止 —— 免得发行页挂着上一版的文案。
