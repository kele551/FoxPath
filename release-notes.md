狐径 FoxPath v1.0.0 — 首个版本

## 下载

- `狐径-v1.0.0.exe`（8.77 MB）— 绿色单文件，无需安装，双击即用

## 这个版本能做什么

- **绕过 DNS**：不查系统 DNS，直连实测能握手的 GitHub 官方 IP
- **自动体检**：每 5 分钟重测候选池，死 IP 自动换；候选池定期从 `api.github.com/meta` 刷新
- **自愈**：候选全灭时依次重拉官方表 → 查系统 DNS → 放宽超时重测
- **退出安全**：退出自动还原系统代理；PAC 带 `DIRECT` 兜底，程序没运行时不会让 GitHub 更糟
- **免管理员**：PAC 和系统代理都写在 HKCU，普通用户权限即可

## 用法

1. 下载 exe，双击运行
2. 浏览器自动打开控制面板 `http://127.0.0.1:8788/ui`
3. 勾选「开机自启」即可后台常驻

首次启动请等 15~20 秒（要并发体检 49 个候选 IP），体检完成前代理尚未完全生效。

## 已知边界

- 只代理 `github.com`；`githubassets.com` / `githubusercontent.com` 直连本来就通，走代理反而连不上
- 不能用 `nohup ... &` 在 shell 里后台启动，那样命令结束进程会被终止

反馈：75219857@qq.com
